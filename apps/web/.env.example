# For local development

# Anthropic API Key (required for chat)
ANTHROPIC_API_KEY=sk-ant-...

# Database connection (for local dev, use a local postgres or connection string)
DATABASE_URL=postgresql://postgres:password@localhost:5432/able

# Or use individual env vars (used in AWS ECS)
# DATABASE_HOST=localhost
# DATABASE_USERNAME=postgres
# DATABASE_PASSWORD=password
# DATABASE_NAME=able
